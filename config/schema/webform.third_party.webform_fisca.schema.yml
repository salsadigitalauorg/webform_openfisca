webform.admin_settings.third_party.webform_openfisca:
  type: mapping
  label: 'OpenFisca'
  mapping:
    fisca_enabled:
      type: boolean
      label: 'OpenFisca integration enabled'
    fisca_api_endpoint:
      type: string
      label: 'OpenFisca integration endpoint'

webform.settings.third_party.webform_openfisca:
  type: mapping
  label: 'OpenFisca'
  mapping:
    fisca_enabled:
      type: string
      label: 'OpenFisca integration enabled'
    fisca_api_endpoint:
      type: string
      label: 'OpenFisca integration endpoint'
    fisca_field_mappings:
      type: string
      label: 'JSON encoded field mappings'
    fisca_immediate_response_mapping:
      type: string
      label: 'JSON encoded immediate response mapping'
    fisca_immediate_exit_mapping:
      type: string
      label: 'OpenFisca return keys for immediate exit'
    fisca_immediate_response_ajax_indicator:
      type: boolean
      label: 'Display Ajax indicator for immediate response'
